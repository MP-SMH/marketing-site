import TopNavigation from './TopNavigation';

// AppNavigation - placeholder-compatible app navigation wrapper for shared system.
export default function AppNavigation(props) {
  return <TopNavigation {...props} />;
}
